Question -> "string"
department -> "string" // string can be "all" also which says that this poll is open throughout all departments
OneLiner -> "string"
Votes -> list of [string,int] // "where string is entryNumber", "We append the entryNumber as when voted",
				"int is +1/-1 depending whether user has added a vote or if changed a vote"
